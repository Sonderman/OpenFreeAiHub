enum ServiceStatus { active, maintenance, inactive, testing }

enum ImageGenerationMods { text2Image, image2Image }

enum CategoryTypes { chat, imageGeneration, audioGeneration, videoGeneration }

enum ToolCallStatus { success, error, pending, processing, cancelled }

enum ToolCallType { imageGeneration, imageEditing, webSearch }
